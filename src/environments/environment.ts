export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
  // Google Maps JS API key (restricted to this site's domain) + Place ID, used to show
  // live Google reviews on the landing page. Leave empty to keep the "see reviews" link.
  googleMapsApiKey: '',
  googlePlaceId: '',
};
